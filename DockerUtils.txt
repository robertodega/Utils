--  Avvio immagine docker   ->  docker run -it DOCKER_IMAGE /bin/bash
--  Avvio container         ->  docker exec -it CONTAINER_ID bash
--  Stop container          ->  docker stop CONTAINER_ID - se non funziona, entrare nel container (docker exec -it CONTAINER_ID bash) e digitare 'kill 1'
